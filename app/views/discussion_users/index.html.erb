<h1>Listing discussion_users</h1>

<table>
  <tr>
    <th>Debate</th>
    <th>Usuario</th>
    <th>Data resposta</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @discussion_users.each do |discussion_user| %>
  <tr>
    <td><%= discussion_user.debate_id %></td>
    <td><%= discussion_user.usuario_id %></td>
    <td><%= discussion_user.data_resposta %></td>
    <td><%= discussion_user.status %></td>
    <td><%= link_to 'Show', discussion_user %></td>
    <td><%= link_to 'Edit', edit_discussion_user_path(discussion_user) %></td>
    <td><%= link_to 'Destroy', discussion_user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Discussion user', new_discussion_user_path %>
