<h1>Listing admin_contacts</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Email</th>
    <th>Texto</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_contacts.each do |admin_contact| %>
  <tr>
    <td><%= admin_contact.nome %></td>
    <td><%= admin_contact.email %></td>
    <td><%= admin_contact.texto %></td>
    <td><%= admin_contact.status %></td>
    <td><%= link_to 'Show', admin_contact %></td>
    <td><%= link_to 'Edit', edit_admin_contact_path(admin_contact) %></td>
    <td><%= link_to 'Destroy', admin_contact, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Contact', new_admin_contact_path %>
